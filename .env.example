# Optional local Discogs credentials for manual OAuth/auth console checks.
#
# The automated validation gate does not require these values. Keep real
# credentials in your shell, user secrets, or another local-only secret store.

DISCOGS_CONSUMER_KEY=
DISCOGS_CONSUMER_SECRET=
DISCOGS_TOKEN=
DISCOGS_TOKEN_SECRET=
