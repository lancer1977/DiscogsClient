# Optional local Discogs credentials for manual live API checks.
#
# The default validation gate is deterministic and does not require these
# values. Keep real credentials in your shell, user secrets, or another
# local-only secret store.

DISCOGS_LIVE_TESTS=false
DISCOGS_CONSUMER_KEY=
DISCOGS_CONSUMER_SECRET=
DISCOGS_TOKEN=
DISCOGS_TOKEN_SECRET=
